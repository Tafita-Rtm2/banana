{
  "name": "puppeteer-scraper-proxy",
  "version": "1.0.0",
  "description": "Proxy that uses Puppeteer to automate a webpage (owned by you) to submit an image URL + prompt and returns the generated image",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "axios": "^1.4.0",
    "express": "^4.18.2",
    "dotenv": "^16.3.1",
    "puppeteer": "^20.8.0"
  },
  "license": "MIT"
}
